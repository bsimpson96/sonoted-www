---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class:list={['px-6 py-24 md:px-8', className]}>
  <div class="mx-auto max-w-[1200px]">
    <slot />
  </div>
</section>
